import * as path from "path";

export const INPUT_CSV_DIR = path.join(
  process.cwd(),
  "csv/inputCSV"
);
export const OUTPUT_CSV_DIR = path.join(
  process.cwd(),
  "csv/outputCSV"
);
export const INPUT_IMAGE_DIR = path.join(
  process.cwd(),
  "csv/inputImage"
);
export const OUTPUT_IMAGE_DIR = path.join(
  process.cwd(),
  "csv/outputImage"
);
